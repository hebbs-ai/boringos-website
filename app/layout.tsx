import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BoringOS — The boring way to build agentic platforms",
  description: "Takes away all the boring parts of building agentic platforms. Agent execution, workflows, connectors, memory, drive — all handled.",
  openGraph: {
    title: "BoringOS",
    description: "The boring way to build agentic platforms.",
    url: "https://boringos.dev",
    siteName: "BoringOS",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
